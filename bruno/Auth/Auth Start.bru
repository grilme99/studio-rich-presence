meta {
  name: Auth Start
  type: http
  seq: 1
}

post {
  url: {{host}}/auth/start
  body: none
  auth: inherit
}

script:post-response {
  const code = res.body.code;
  bru.setVar("authFlowCode", code);
}

settings {
  encodeUrl: true
  timeout: 0
}
